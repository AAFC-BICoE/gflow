[library]

# If library with this name doesn't exist one will be created
library = Test Library 2

[input]

# local, server, url, or galaxyfs
dataset_src = localllll
num_datasets = 1
data_0 = data/SNPs.bed
label_0 = Features

[output]

output_history_name = Test History 2

[workflow]

# local, id, or shared
source = local
workflow = data/sort.ga

[tools_params]

num_tools = 0