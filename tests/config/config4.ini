[galaxy]

galaxy_url =
galaxy_key = d2e3a6e000eddd713a15307e3bedfd61

[library]

# If library with this name doesn't exist one will be created
library = Exons and SNPs again

[input]

# local, url, or galaxyfs
dataset_src = local
num_datasets = 2
data0 = data/exons.bed
label0 = Exons
data1 = data/SNPs.bed
label1 = Features

[output]

output_history_name = Histy_new

[workflow]

# local, id, or shared
source = id
workflow = f597429621d6eb2b

[tool_params]

num_tools = 2
tool_0 = 8
num_params_0 = 1
param_label_0-0 = lineNum
param_0-0 = 10